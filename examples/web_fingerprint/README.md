Performs simple HTTP requests to fingerprint the technologies used in a target website.

### Example Usage

```sh
nerve -G "openai://gpt-4" -T web_fingerprint -D "HTTP_TARGET=http://testphp.vulnweb.com/"
```